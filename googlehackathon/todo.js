(function(){
    //data is a parsed JSON object
    function FillToDoList(data, destination)
    {
        for(taskId in data.tasks)
        {
            var crTask = data.tasks[taskId];
            
        }
    }
})()